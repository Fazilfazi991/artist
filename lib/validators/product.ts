import { z } from 'zod';
import { priceSchema, slugSchema, uuidSchema } from './shared';

export const productSchema = z.object({ seller_id: uuidSchema, category_id: uuidSchema, name: z.string().min(2), slug: slugSchema, short_description: z.string().max(240).optional(), description: z.string().optional(), product_type: z.enum(['ready_to_ship','customized','bespoke']), base_price: priceSchema.optional(), stock_quantity: z.coerce.number().int().min(0).optional(), dispatch_days: z.coerce.number().int().min(0).optional(), production_days: z.coerce.number().int().min(0).optional(), shipping_fee: priceSchema.default(0), is_customizable: z.boolean().default(false) }).superRefine((value, ctx) => { if (value.product_type === 'ready_to_ship' && value.stock_quantity == null) ctx.addIssue({ code: 'custom', path: ['stock_quantity'], message: 'Ready-to-ship products require stock quantity.' }); if (value.product_type === 'customized' && value.production_days == null) ctx.addIssue({ code: 'custom', path: ['production_days'], message: 'Customized products require production days.' }); if (value.product_type !== 'bespoke' && value.base_price == null) ctx.addIssue({ code: 'custom', path: ['base_price'], message: 'Non-bespoke products require a base price.' }); });
export const productUpdateSchema = productSchema.partial();
