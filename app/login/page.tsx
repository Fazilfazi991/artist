import Link from "next/link";
import { SectionHeading } from "@/components/ui";
import { loginAction } from "@/app/auth/actions";
export default async function LoginPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <main className="mx-auto max-w-md px-4 py-14 sm:px-6"><SectionHeading eyebrow="Login" title="Welcome back" copy="Log in to track purchases, continue seller onboarding, or manage your account." />{params.error ? <p className="mb-4 rounded-lg border border-rust/30 bg-rust/10 p-3 text-sm font-bold text-rust">{params.error}</p> : null}<form action={loginAction} className="grid gap-4 rounded-xl border border-line bg-white p-6 shadow-soft">{params.next ? <input type="hidden" name="next" value={params.next} /> : null}<input name="email" className="min-h-12 rounded-lg border border-line px-4 outline-none focus:border-rust" placeholder="Email" type="email" required /><input name="password" className="min-h-12 rounded-lg border border-line px-4 outline-none focus:border-rust" placeholder="Password" type="password" required /><button className="rounded-lg bg-rust px-5 py-3 font-black text-white transition hover:bg-rust-hover">Log in</button><div className="flex flex-wrap justify-between gap-3 text-sm font-bold"><Link href="/register" className="text-rust">Create buyer account</Link><Link href="/seller/register" className="text-sage">Register as seller</Link></div></form></main>; }
