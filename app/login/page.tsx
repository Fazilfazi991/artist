import Link from "next/link";
import { SectionHeading } from "@/components/ui";
import { loginAction } from "@/app/auth/actions";
export default async function LoginPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <main className="mx-auto max-w-md px-4 py-14 sm:px-6"><SectionHeading eyebrow="Login" title="Welcome back" copy="Log in to track purchases, apply as a seller, or review applications." />{params.error ? <p className="mb-4 rounded-lg border border-rust/30 bg-rust/10 p-3 text-sm font-bold text-rust">{params.error}</p> : null}<form action={loginAction} className="grid gap-4 rounded-lg border border-line bg-white p-6">{params.next ? <input type="hidden" name="next" value={params.next} /> : null}<input name="email" className="rounded-lg border border-line px-4 py-3" placeholder="Email" type="email" required /><input name="password" className="rounded-lg border border-line px-4 py-3" placeholder="Password" type="password" required /><button className="rounded-lg bg-rust px-5 py-3 font-black text-white">Log in</button><Link href="/register" className="text-sm font-bold text-teal">Create buyer account</Link></form></main>; }
