import { SectionHeading } from "@/components/ui";
const flows = [{ title: "Customized products", copy: "Text, notes, options, and reference uploads before checkout." }, { title: "Bespoke consultation", copy: "Requirements, budget, quantity, location, quote, deposit, milestones, and final payment tracking." }, { title: "Progress visibility", copy: "Buyer and seller timelines stay clear from request through completion." }];
export default function CustomOrdersPage() { return <main className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><SectionHeading eyebrow="Custom orders" title="Built for handmade workflows" copy="The platform keeps ready-to-ship, customized, and bespoke products separate from the beginning." /><div className="grid gap-4 md:grid-cols-3">{flows.map((flow) => <article key={flow.title} className="rounded-lg border border-line bg-white p-6"><h2 className="text-xl font-black">{flow.title}</h2><p className="mt-3 leading-7 text-muted">{flow.copy}</p></article>)}</div></main>; }
