import Link from "next/link";
import { SectionHeading } from "@/components/ui";
import { registerAction } from "@/app/auth/actions";
export default async function RegisterPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <main className="mx-auto max-w-md px-4 py-14 sm:px-6"><SectionHeading eyebrow="Register" title="Create your buyer account" copy="Create an account to save products and apply as an artisan seller." />{params.error ? <p className="mb-4 rounded-lg border border-rust/30 bg-rust/10 p-3 text-sm font-bold text-rust">{params.error}</p> : null}<form action={registerAction} className="grid gap-4 rounded-lg border border-line bg-white p-6"><input name="fullName" className="rounded-lg border border-line px-4 py-3" placeholder="Full name" required /><input name="phone" className="rounded-lg border border-line px-4 py-3" placeholder="Phone" required /><input name="email" className="rounded-lg border border-line px-4 py-3" placeholder="Email" type="email" required /><input name="password" className="rounded-lg border border-line px-4 py-3" placeholder="Password" type="password" required minLength={8} /><input name="confirmPassword" className="rounded-lg border border-line px-4 py-3" placeholder="Confirm password" type="password" required minLength={8} /><button className="rounded-lg bg-rust px-5 py-3 font-black text-white">Register</button><Link href="/become-a-seller" className="text-sm font-bold text-teal">Apply as a seller after registering</Link></form></main>; }
