import Link from 'next/link';
import { notFound } from 'next/navigation';
import { SectionHeading } from '@/components/ui';
import { getStorefrontContext } from '@/lib/storefront/get-storefront-context';
export const dynamic = 'force-dynamic';
export default async function ArtisanCollectionDetailPage({ params }: { params: Promise<{ storeSlug: string; collectionSlug: string }> }) { const { storeSlug, collectionSlug } = await params; const context = await getStorefrontContext(storeSlug); if (!context) notFound(); const collection = context.collections.find((item:any)=>item.slug===collectionSlug); if(!collection) notFound(); const productIds = new Set((collection.seller_collection_products || []).map((item:any)=>item.product_id)); const products = context.products.filter((product:any)=>productIds.has(product.id)); return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><SectionHeading eyebrow={context.seller.store_name} title={collection.name} copy={collection.description || 'Curated storefront collection.'} /><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map((product:any)=><Link key={product.id} href={`/artisan/${storeSlug}/product/${product.slug}`} className="rounded-lg border border-line bg-white"><img src={product.product_images?.[0]?.image_url || '/artisan-hero.png'} alt={product.name} className="aspect-square w-full rounded-t-lg object-cover"/><div className="p-4"><h2 className="font-black">{product.name}</h2></div></Link>)}</div></main> }