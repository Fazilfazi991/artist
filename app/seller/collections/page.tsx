import Link from 'next/link';
import { SectionHeading, Badge } from '@/components/ui';
import { createClient } from '@/lib/supabase/server';
import { requireApprovedSeller } from '@/lib/services/auth';
import { deleteCollectionAction } from './actions';
export const dynamic = 'force-dynamic';
export default async function CollectionsPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; const seller = await requireApprovedSeller(); const supabase = await createClient(); const { data: collections } = await supabase.from('seller_collections').select('*, seller_collection_products(product_id)').eq('seller_id', seller.id).order('display_order'); return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><SectionHeading eyebrow="Storefront" title="Collections" copy="Create storefront collections that are separate from marketplace categories." action={<Link href="/seller/collections/new" className="rounded-lg bg-rust px-5 py-3 font-black text-white">New Collection</Link>} />{params.saved ? <p className="mb-4 rounded-lg border border-success/30 bg-sage/10 p-3 text-success">Collection saved.</p> : null}<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{(collections || []).map((collection:any)=><article key={collection.id} className="rounded-xl border border-line bg-white p-5"><div className="flex items-center gap-2"><h2 className="font-black">{collection.name}</h2>{collection.is_featured?<Badge>Featured</Badge>:null}</div><p className="mt-2 text-sm text-muted">{collection.description || 'No description yet.'}</p><p className="mt-2 text-xs text-muted">{collection.seller_collection_products?.length || 0} products</p><div className="mt-4 flex gap-2"><Link href={`/seller/collections/${collection.id}/edit`} className="rounded-lg border border-line px-3 py-2 text-sm font-black">Edit</Link><form action={deleteCollectionAction}><input type="hidden" name="collection_id" value={collection.id}/><button className="rounded-lg border border-line px-3 py-2 text-sm font-black">Delete</button></form></div></article>)}</div>{!collections?.length ? <div className="rounded-xl border border-line bg-white p-10 text-center text-muted">No collections yet.</div> : null}</main> }